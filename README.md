## Hardware Components

ESP model nos:

- `esp32-c6-wroom-1`
- `esp-eye`

Pump

- `(unknown)`

Servos:

- `DIYMORE-DM996 (x2)`

Water sensors:

- `(unknown)`

---

# Setup Guide

install the ESP-IDF vscode extension
should be prompted to install the ESP-IDF installation manager
