Model no: `esp32-c6-wroom-1`

#Setup Guide

install the ESP-IDF vscode extension
should be prompted to install the ESP-IDF installation manager

<!-- prereqon mac `brew install cmake ninja dfu-util` -->
