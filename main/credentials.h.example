/*
 * credentials.h.example
 *
 * Copy this file to `main/credentials.h`, fill in your real values,
 * and keep `main/credentials.h` out of version control (it's already
 * listed in .gitignore).
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Wi-Fi credentials
#define WIFI_SSID "Your_SSID_here"
#define WIFI_PASS "Your_WiFi_Password_here"

// MQTT / Broker address
// Example forms: "user@192.0.2.1" or "mqtt://192.0.2.1:1883"
#define MQTT_URI "your_user@broker_ip_or_uri"

// Topics
#define TOPIC_SUB "test/esp32c6/sub"
#define TOPIC_PUB "test/esp32c6/pub"

#endif // CREDENTIALS_H
